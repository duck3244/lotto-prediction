# 백엔드 의존성 핀
#
# - TensorFlow 2.15.x 로 고정: model.py 가 사용하는
#   ``from tensorflow.keras.models import Sequential`` 등 Keras 2 임포트 경로는
#   TF 2.13~2.15 에서만 보장된다. TF 2.16+ 부터 Keras 3 로 분리되며 일부
#   API/임포트 경로가 바뀐다. RTX 4060(sm_89, CUDA 12) 호환도 2.15 가 가장
#   매끄럽다. (참고: TF 2.15 권장 조합 = CUDA 12.2 / cuDNN 8.9)
# - NumPy 는 2.0 이전으로 잠근다(TF/pandas/sklearn ABI 호환).
# - 그 외는 합리적 상한만 둔다.
numpy>=1.23,<2.0
pandas>=1.5,<3.0
tensorflow~=2.15.0
scikit-learn>=1.2,<2.0
matplotlib>=3.6,<4.0
openpyxl>=3.1,<4.0

# --- FastAPI 단일 사용자 MVP ---------------------------------------------
fastapi~=0.115
uvicorn[standard]~=0.30
pydantic~=2.8
python-multipart~=0.0.9

# --- Dev / 테스트 -------------------------------------------------------
pytest~=8.3
httpx~=0.27  # FastAPI TestClient 의존
